# Таблица маршрутизации запросов

## Классификация запроса по категориям

| Категория | Признаки | Фаза |
|-----------|----------|------|
| **Сбор информации** | "посмотри", "что тут", "разберись", путь к файлу | `/gather` |
| **Требования** | "хочу фичу", "нужно чтобы", "user story", бизнес-логика | `/product` |
| **Архитектура** | "как построить", "какой паттерн", "структура", "контракты" | `/architect` |
| **Дизайн UX** | "как будет выглядеть", "user flow", "wireframe", "UX" | `/design` |
| **UI спецификация** | "экраны", "состояния", "компоненты", "State/Action" | `/ui-spec` |
| **Генерация кода (client)** | "напиши", "реализуй", "сделай" + UI/mobile/frontend | `/gen` |
| **Генерация кода (server)** | "напиши", "реализуй", "сделай" + backend/API/server | `/gen-server` |
| **Ревью** | "проверь", "ревью", "что не так с кодом" | `/code-review` |
| **Дебаг (stacktrace)** | "ошибка", "краш", stacktrace, exception | `/debug` |
| **Расследование** | "посмотри что не так с", "разберись почему", "проверь /api/...", проблема без stacktrace | `/investigate` |
| **Тест-план** | "тест-план", "стратегия тестирования", "какие тесты", "test plan" | `/test-plan` |
| **Рефакторинг** | "отрефактори", "переструктурируй", "технический долг" | `/refactor` |
| **Миграции** | "миграция", "схема БД", "новая таблица" | `/migrate` |
| **Безопасность** | "безопасность", "уязвимость", "OWASP", "аудит" | `/security` |
| **API расхождения** | "DTO не совпадают", "client/server рассинхрон" | `/api-diff` |
| **Changelog** | "что изменилось", "release notes", "changelog" | `/changelog` |
| **Релиз** | "готовим релиз", "деплой", "чеклист" | `/release` |
| **Верификация** | "проверь билд", "компилируется?", "всё работает?", "verify" | `/verify` |
| **Статус** | "что сделано", "прогресс", "где мы" | `/progress` |
| **Передача контекста** | "передай контекст", "handoff", "для следующего чата" | `/handoff` |
| **Быстрый старт** | "начни с нуля", описание фичи без конкретной фазы | `/kickoff` |
| **Настройка проекта** | "новый проект", "инициализация", "настрой конвейер" | `/new-project` |

## Автоопределение следующей фазы

Если пользователь НЕ указал конкретную задачу или спрашивает "что дальше":

### Приоритет 1: Блокеры (RE-PASS)
Если в `ai/status.md` есть `⚠️ RE-PASS` — предложи указанную фазу:
> "В status.md есть блокер: RE-PASS `/architect` (причина: ...). Рекомендую начать с него."

### Приоритет 2: Прогресс артефактов
Определи первый незаполненный артефакт в цепочке и предложи соответствующую фазу:

```
context [ ] → /gather
requirements [ ] → /product
architecture [ ] → /architect
ui_spec [ ] → /design + /ui-spec
test_plan [ ] → /test-plan
tasks [x] + implementation [ ] → /gen (или /gen-server)
implementation [x] + review [ ] → /code-review
всё [x] → /release (или /changelog)
```

### Приоритет 3: Next action из status.md
Если в `ai/status.md` указан "Next action" — предложи его.

### Формат предложения
```
Текущая фаза: [фаза из status.md]
Прогресс: N/M артефактов заполнено
Рекомендую: `/фаза` — [почему]
```
