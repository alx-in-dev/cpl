# Проверки артефактов по фазам

Для каждого артефакта, который должен быть заполнен на текущей фазе:

## Структурная проверка
- Файл существует и не пустой
- Обязательные секции присутствуют (не placeholder-комментарии)
- Нет секций с `...` или `TODO` без конкретики

## Содержательная проверка (по фазе)

### После `/gather`:
- `ai/context.md`: есть описание "Что строим", ограничения, текущее состояние

### После `/product`:
- `ai/requirements.md`: есть хотя бы 1 user story с acceptance criteria, есть non-goals
- `ai/tasks.md`: есть хотя бы 1 таск с чекбоксом `[ ]`

### После `/architect`:
- `ai/architecture.md`: есть ADR (хотя бы 1 решение), есть компоненты, есть data flow
- `ai/api_contracts.md` (если backend/fullstack): есть хотя бы 1 эндпоинт с DTO

### После `/design`:
- `ai/design.md`: есть user flows, есть wireframes (ASCII или ссылка на .pen)

### После `/ui-spec`:
- `ai/ui_spec.md`: есть экраны со state-моделью

### После `/test-plan`:
- `ai/test_plan.md`: есть таблицы тестов хотя бы для 1 слоя

### После `/gen` или `/gen-server`:
- `ai/tasks.md`: хотя бы 1 таск отмечен `[x]`
- `ai/impl_notes.md`: есть записи в "Выполнено"

### После `/code-review`:
- `ai/review.md`: есть секция Critical/Warning/Nit (даже если пустая = всё ОК)

## Проверка консистентности между артефактами

- Таски в `ai/tasks.md` соответствуют требованиям из `ai/requirements.md`
- Архитектура в `ai/architecture.md` покрывает все компоненты из `ai/tasks.md`
- API контракты (если есть) согласованы с архитектурой
- Нет противоречий между артефактами

## Проверка RE-PASS блокеров

Если в `ai/status.md` или `ai/review.md` или `ai/investigate.md` есть секция "Требуется re-pass":
- Блокер разрешён? (фаза была перезапущена, артефакт обновлён)
- Если не разрешён — добавь в отчёт как BLOCKER
